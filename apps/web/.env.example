# Frontend environment variables.
# In local dev you can leave this unset — the Vite proxy forwards /api to :8000.
#
# In production (Vercel) set this to your deployed backend API base, including /api:
#   VITE_API_BASE_URL=https://flowpilot-api.onrender.com/api
VITE_API_BASE_URL=
